# --- Core data stack ---
pandas>=2.2.2
numpy>=1.26.4
pyarrow>=15.0.0        # Parquet engine
openpyxl>=3.1.2        # Excel (.xlsx) support
requests>=2.31.0       # Download from URL if needed
rich>=13.7.1           # Nicer CLI output (optional but used)

# --- Analytics / anomalies ---
scikit-learn>=1.3.2    # IsolationForest (multivariate anomalies)
scipy>=1.11.4          # Stats helpers (optional but common)

# --- Optional LLM insights (uncomment if using --insights) ---
# openai>=1.40.0
# anthropic>=0.34.0

# --- Optional Parquet alternative (not required if using pyarrow) ---
# fastparquet>=2024.2.0
